const view = require('../../src/view');

// view.renderBlog() should return html of the blog page

// the title "microblog" should be displayed on the page

// posts provided to renderBlog should be rendered in divs 

// a new message form should be displayed on the page
