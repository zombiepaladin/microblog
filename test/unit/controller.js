const controller = require('../../src/controller');

// controller.list() should list all posts in the model

// controller.create() should create a new post and add it to the model
// before serving a list of all posts 
