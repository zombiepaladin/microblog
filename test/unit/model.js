const model = require('../../src/model.js');

// getAllPosts() should return an array of all messages

// createPost() should add a new message to the Array

// createPost() should NOT add a message more than 255 characters in length
